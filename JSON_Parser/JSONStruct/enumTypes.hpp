/**
 * \brief Enum header holding the different types of JSON objects
 * \author Elizabet Velikova
 */

#ifndef ENUM_H
#define ENUM_H

//!< Enum holding the specific types
enum Type {
    JSONSTRING,
    JSONNUMBER,
    JSONBOOL,
    JSONARRAY,
    JSONOBJECT
};

#endif